---
title: Overview
description: Shared binding, command and sequence engine for OpenTUI hosts
order: 1
navTitle: Overview
skill:
  entry: true
  intents: [keymap, keybindings, shortcuts, commands, leader, ex-commands]
---

# Keymap

OpenTUI Keymap is a host-agnostic binding engine for terminals and the browser. It gives you layered shortcuts, discoverable commands, sequence handling and framework-ready state so one key system can drive the whole app.

A binding maps a `key` or key sequence to a `cmd`:

```ts
[
  { key: "x", cmd: "save-file" },
  { key: "ctrl+x", cmd: "cut" },
  { key: "dd", cmd: "delete-line" },
  { key: "<leader>s", cmd: ":write session.log" },
  { key: "?", cmd: "toggle-help" },
  { key: { name: "return", ctrl: true }, cmd: "submit" },
]
```

- The default parser accepts single-stroke named keys, modifier chords, literal punctuation and concatenated multi-stroke sequences.

<img src="/keymap-engine-overview.svg" alt="Keymap Engine Concept Overview" style="width: 100%; margin: 1.5rem 0; display: block;" />

## Register

Register time is when you shape the keymap by calling APIs such as `registerLayer()`, `registerToken()` and addon registration helpers. This phase writes the compiled model that later dispatch and queries use.

At this stage, the keymap turns declarations into compiled state:

1. Register layers, tokens and addons.
2. Expand, parse, transform and normalize binding input.
3. Compile fields into requirements, attrs and matchers.
4. Validate and store commands and compiled binding sequences.

- Binding strings are expanded, parsed, transformed and normalized here.
- Layer, binding and command fields compile here.
- Command definitions are validated and indexed here.
- Environment changes such as token registration can recompile affected layers.

## Dispatch

Dispatch time starts when the host reports focus changes or input. This phase reads the same compiled model through current state, then may update pending-sequence state or runtime data.

On each input event, the keymap does this:

1. Read host focus state and the incoming input event.
2. Select the active layers for that focus state.
3. Match an exact binding or continue a pending sequence.
4. Resolve the winning command by precedence.
5. Run the command and refresh keymap state.

- Target scoping, priority, enabled matchers and runtime data are evaluated here.
- Commands and intercepts can update runtime data and pending-sequence state during dispatch.

## Query

Query time is when application code asks the keymap what is currently available. This phase reads the same compiled model through current focus, target liveness, pending-sequence state, runtime data and enabled matchers.

At this stage, the keymap does this:

1. Start from the compiled layers, bindings and commands.
2. Apply current focus, target scoping, priority, pending-sequence state, enabled matchers and runtime data.
3. Return derived views such as active keys, pending sequence, commands and command entries.

- `getActiveKeys()` is a query over the current state, not just a dump of registered bindings.
- `getCommands()` and `getCommandEntries()` can project reachable, active, or registered command views.
- Query results change when focus, pending sequence, runtime data, or enabled matchers change.

All entrypoints below use the same registration, query and dispatch model. They only differ in how they provide targets, focus and input events.

Start with the page that matches your host or framework:

- [Core](/docs/keymap/core) - bare `Keymap` and `KeymapHost`
- [OpenTUI](/docs/keymap/opentui) - `CliRenderer` / `Renderable` host adapter
- [HTML](/docs/keymap/html) - `HTMLElement` / `KeyboardEvent` host adapter
- [React](/docs/keymap/react) - provider and hooks for a pre-created OpenTUI keymap
- [Solid](/docs/keymap/solid) - provider and hooks for a pre-created OpenTUI keymap
- [Addons](/docs/keymap/addons) - shipped addons plus addon authoring

## Entry points

| Package | Description |
| --- | --- |
| `@opentui/keymap` | Bare engine: `Keymap`, key stringifiers and shared types for custom hosts |
| `@opentui/keymap/addons` | Universal addons for parser stages, metadata, diagnostics and sequences |
| `@opentui/keymap/addons/opentui` | Universal addons plus OpenTUI-specific base-layout and edit-buffer helpers |
| `@opentui/keymap/opentui` | OpenTUI host adapter for terminal apps built on `@opentui/core` |
| `@opentui/keymap/html` | DOM host adapter for browser UIs rooted in an `HTMLElement` |
| `@opentui/keymap/react` | React provider and hooks for an OpenTUI keymap |
| `@opentui/keymap/solid` | Solid provider and hooks for an OpenTUI keymap |

## Vocabulary

| Concept | Description |
| --- | --- |
| Host | Adapts focus, hierarchy, lifecycle and key events into the `KeymapHost` contract |
| Layer | Groups bindings, commands, optional target scoping, priority and custom fields |
| Binding | Maps a key or key sequence to a string command or inline handler |
| Command | Named handler with registration fields and optional compiled attrs |
| Token | Named single-stroke alias used in binding strings such as `<leader>` |
| Runtime data | Mutable key-value store used by matchers, commands and intercepts |
| Pending sequence | The currently matched prefix when a sequence is in progress |
| Active key | View of what can run next from the current focus and pending state |

## Bare vs default helpers

| Helper | What it installs |
| --- | --- |
| `new Keymap(host)` | Bare engine only |
| `createOpenTuiKeymap(renderer)` | Bare engine plus the OpenTUI host adapter |
| `createHtmlKeymap(root)` | Bare engine plus the HTML host adapter |
| `createDefaultOpenTuiKeymap(...)` | OpenTUI keymap plus default keys, enabled fields and metadata fields |
| `createDefaultHtmlKeymap(...)` | HTML keymap plus default keys, enabled fields, metadata fields and event matching |

The default helpers intentionally stay small. Leader tokens, ex commands, timed disambiguation, warning analyzers and OpenTUI textarea helpers are separate addons.

## Next

- [Core](/docs/keymap/core) documents the shared runtime, queries, intercepts and extension points.
- [Addons](/docs/keymap/addons) covers the shipped addons and how to build your own on top of the public APIs.
- [OpenTUI](/docs/keymap/opentui), [HTML](/docs/keymap/html), [React](/docs/keymap/react) and [Solid](/docs/keymap/solid) cover the host/framework-specific surfaces.

Examples: `packages/core/src/examples/keymap-demo.ts`, `packages/keymap/examples/keymap-html/app.ts`, `packages/react/examples/keymap.tsx`, `packages/solid/examples/components/keymap-demo.tsx`

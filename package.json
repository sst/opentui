{
  "name": "@opentui",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/solid/examples"
  ],
  "scripts": {
    "build": "cd packages/core && bun run build && cd ../three && bun run build && cd ../solid && bun run build && cd ../react && bun run build && cd ../keymap && bun run build",
    "build:examples": "cd packages/examples && bun run build",
    "clean": "bun scripts/clean.ts",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint .",
    "lint:ci": "oxlint . --deny-warnings",
    "lint:fix": "oxlint --fix .",
    "pre-publish": "bun scripts/pre-publish.ts",
    "publish": "bun run pre-publish && bun run publish:core && bun run publish:three && bun run publish:react && bun run publish:solid && bun run publish:keymap",
    "publish:core": "cd packages/core && bun run publish",
    "publish:three": "cd packages/three && bun run publish",
    "publish:react": "cd packages/react && bun run publish",
    "publish:solid": "cd packages/solid && bun run publish",
    "publish:keymap": "cd packages/keymap && bun run publish",
    "prepare-release": "bun scripts/prepare-release.ts",
    "test": "bun run --filter '@opentui/core' --filter '@opentui/three' --filter '@opentui/solid' --filter '@opentui/react' --filter '@opentui/keymap' --if-present test"
  },
  "devDependencies": {
    "oxfmt": "0.41.0",
    "oxlint": "1.56.0"
  }
}
